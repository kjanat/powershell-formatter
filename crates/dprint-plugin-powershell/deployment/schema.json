{
  "$defs": {
    "BraceStyle": {
      "description": "Where opening braces of statement blocks go.",
      "oneOf": [
        {
          "const": "sameLine",
          "description": "`if ($x) {` — brace on the same line (K&R / OTBS / Stroustrup).",
          "type": "string"
        },
        {
          "const": "nextLine",
          "description": "Brace on its own line (Allman).",
          "type": "string"
        }
      ]
    },
    "BranchKeywordPlacement": {
      "description": "Whether `else`/`elseif`/`catch`/`finally` cuddle onto the closing brace.",
      "oneOf": [
        {
          "const": "nextLine",
          "description": "`}` then `else {` on the next line (Stroustrup — PSSA default).",
          "type": "string"
        },
        {
          "const": "cuddled",
          "description": "`} else {` (OTBS).",
          "type": "string"
        }
      ]
    },
    "EndOfLine": {
      "description": "Line-ending handling for output.",
      "oneOf": [
        {
          "const": "auto",
          "description": "Keep the input's detected dominant newline style (PSSA behavior).",
          "type": "string"
        },
        {
          "const": "lf",
          "description": "Force `\\n`.",
          "type": "string"
        },
        {
          "const": "crlf",
          "description": "Force `\\r\\n`.",
          "type": "string"
        }
      ]
    },
    "PipelineIndentation": {
      "description": "How pipeline continuation lines are indented, mirroring PSSA's\n`PipelineIndentation`.",
      "oneOf": [
        {
          "const": "increaseIndentationForFirstPipeline",
          "description": "Indent once at the first line-ending pipe of a pipeline.",
          "type": "string"
        },
        {
          "const": "increaseIndentationAfterEveryPipeline",
          "description": "Indent one more level after every line-ending pipe.",
          "type": "string"
        },
        {
          "const": "noIndentation",
          "description": "Continuation lines stay at the pipeline's own level.",
          "type": "string"
        },
        {
          "const": "none",
          "description": "Leave lines after a trailing pipe exactly as written.",
          "type": "string"
        }
      ]
    }
  },
  "$id": "https://plugins.dprint.dev/kjanat/powershell/0.0.0/schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "The complete formatting configuration.\n\nDefaults reproduce PSScriptAnalyzer's `CodeFormatting` preset.",
  "properties": {
    "alignAssignment": {
      "default": true,
      "description": "Align `=` in multi-member hashtables and enums\n(`PSAlignAssignmentStatement`).",
      "type": "boolean"
    },
    "braceStyle": {
      "$ref": "#/$defs/BraceStyle",
      "default": "sameLine",
      "description": "Opening-brace placement (`PSPlaceOpenBrace.OnSameLine`)."
    },
    "branchKeywordPlacement": {
      "$ref": "#/$defs/BranchKeywordPlacement",
      "default": "nextLine",
      "description": "Branch keyword placement after `}`\n(`PSPlaceCloseBrace.NewLineAfter`, inverted sense)."
    },
    "collapseSpaceAroundPipe": {
      "default": false,
      "description": "Also collapse redundant spaces around `|`\n(`PSUseConsistentWhitespace.CheckPipeForRedundantWhitespace`).",
      "type": "boolean"
    },
    "collapseSpaceBetweenParameters": {
      "default": false,
      "description": "Collapse runs of spaces between command elements\n(`PSUseConsistentWhitespace.CheckParameter`).",
      "type": "boolean"
    },
    "commandCasing": {
      "default": true,
      "description": "Correct command/parameter casing via the injected\n[`crate::CommandCatalog`] (`PSUseCorrectCasing.CheckCommands`).",
      "type": "boolean"
    },
    "endOfLine": {
      "$ref": "#/$defs/EndOfLine",
      "default": "auto",
      "description": "Output newline handling."
    },
    "finalNewline": {
      "default": null,
      "description": "Ensure the output ends with exactly one final newline. `None`\npreserves the input's final-newline state (PSSA behavior).",
      "type": [
        "boolean",
        "null"
      ]
    },
    "ignoreAssignmentInHashtable": {
      "default": true,
      "description": "Skip `=` inside multi-line hashtables when checking operator spacing\n(`PSUseConsistentWhitespace.IgnoreAssignmentOperatorInsideHashTable`).",
      "type": "boolean"
    },
    "ignoreOneLineBlock": {
      "default": true,
      "description": "Leave one-line `{ ... }` blocks alone\n(`PSPlaceOpenBrace/PSPlaceCloseBrace.IgnoreOneLineBlock`).",
      "type": "boolean"
    },
    "indentWidth": {
      "default": 4,
      "description": "Spaces per indentation level (ignored when `use_tabs`).",
      "format": "uint8",
      "maximum": 255,
      "minimum": 0,
      "type": "integer"
    },
    "indentation": {
      "default": true,
      "description": "Reindent lines (`PSUseConsistentIndentation`).",
      "type": "boolean"
    },
    "keywordCasing": {
      "default": true,
      "description": "Lowercase keywords (`PSUseCorrectCasing.CheckKeyword`).",
      "type": "boolean"
    },
    "lineWidth": {
      "default": 120,
      "description": "Target maximum line width. Used by width-aware layout; `0` disables\nwidth-based decisions.",
      "format": "uint16",
      "maximum": 65535,
      "minimum": 0,
      "type": "integer"
    },
    "newlineAfterOpenBrace": {
      "default": true,
      "description": "Require a newline after every opening brace\n(`PSPlaceOpenBrace.NewLineAfter`).",
      "type": "boolean"
    },
    "noEmptyLineBeforeCloseBrace": {
      "default": false,
      "description": "Remove blank lines directly before `}`\n(`PSPlaceCloseBrace.NoEmptyLineBefore`).",
      "type": "boolean"
    },
    "operatorCasing": {
      "default": true,
      "description": "Lowercase operators (`PSUseCorrectCasing.CheckOperator`).",
      "type": "boolean"
    },
    "pipelineIndentation": {
      "$ref": "#/$defs/PipelineIndentation",
      "default": "increaseIndentationForFirstPipeline",
      "description": "Pipeline continuation indentation style."
    },
    "placeCloseBrace": {
      "default": true,
      "description": "Run the closing-brace placement rule at all\n(`PSPlaceCloseBrace.Enable`).",
      "type": "boolean"
    },
    "placeOpenBrace": {
      "default": true,
      "description": "Run the opening-brace placement rule at all (`PSPlaceOpenBrace.Enable`).",
      "type": "boolean"
    },
    "spaceAfterKeyword": {
      "default": true,
      "description": "Enforce a space between keywords and `(`\n(`PSUseConsistentWhitespace.CheckOpenParen`).",
      "type": "boolean"
    },
    "spaceAfterSeparator": {
      "default": true,
      "description": "Enforce a space after `,`/`;`\n(`PSUseConsistentWhitespace.CheckSeparator`).",
      "type": "boolean"
    },
    "spaceAroundOperator": {
      "default": true,
      "description": "Enforce single spaces around binary/assignment operators\n(`PSUseConsistentWhitespace.CheckOperator`).",
      "type": "boolean"
    },
    "spaceAroundPipe": {
      "default": true,
      "description": "Add missing spaces around `|` (`PSUseConsistentWhitespace.CheckPipe`).",
      "type": "boolean"
    },
    "spaceBeforeOpenBrace": {
      "default": true,
      "description": "Enforce a space before opening braces\n(`PSUseConsistentWhitespace.CheckOpenBrace`).",
      "type": "boolean"
    },
    "spaceInsideBrace": {
      "default": true,
      "description": "Enforce single spaces just inside one-line braces\n(`PSUseConsistentWhitespace.CheckInnerBrace`).",
      "type": "boolean"
    },
    "useTabs": {
      "default": false,
      "description": "Indent with tabs instead of spaces.",
      "type": "boolean"
    }
  },
  "title": "PowerShell formatter configuration",
  "type": "object"
}
